function sliceTree(start, stop) {
  //does the element at start id have children?
    // yes, then for each child:
      // if its id === stop, add this and all parent elems to start id to result
      // else see if this element has children
    // no, then return;
  // if result empty, return undefined, else, return result
}